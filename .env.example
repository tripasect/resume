# Public origin of the FitCheck API (the Express server in server/index.js).
# Leave empty to call same-origin `/api/*`, which is what nginx/the dev proxy serve.
# Must be inlined at build time, so it is read by `next build`.
NEXT_PUBLIC_API_URL=
